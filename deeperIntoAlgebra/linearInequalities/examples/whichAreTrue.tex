\begin{question}
    Which of the following inequalities are true?
    \begin{selectAll}
        \choice{\(-1 < -2\)}
        \choice[correct]{\(-1 > -2\)}
        \choice{\(-1 \le -2\)}
        \choice[correct]{\(-1 \ge -2\)}
    \end{selectAll}
\end{question}

\begin{question}
    Which of the following inequalities are true?
    \begin{selectAll}
        \choice{\(\sqrt{3} < \sqrt[3]{4}\)}
        \choice[correct]{\(\sqrt{3} > \sqrt[3]{4}\)}
        \choice{\(\sqrt{3} \le \sqrt[3]{4}\)}
        \choice[correct]{\(\sqrt{3} \ge \sqrt[3]{4}\)}
    \end{selectAll}
\end{question}

\begin{question}
    Which of the following inequalities are true?
    \begin{selectAll}
        \choice[correct]{\(\frac{1}{\sqrt{3}} < \frac{1}{\sqrt{2}}\)}
        \choice{\(\frac{1}{\sqrt{3}} > \frac{1}{\sqrt{2}}\)}
        \choice[correct]{\(\frac{1}{\sqrt{3}} \le \frac{1}{\sqrt{2}}\)}
        \choice{\(\frac{1}{\sqrt{3}} \ge \frac{1}{\sqrt{2}}\)}
    \end{selectAll}
\end{question}

\begin{question}
    Which of the following inequalities are true?
    \begin{selectAll}
        \choice{\(\sqrt{16} < \sqrt[3]{64}\)}
        \choice{\(\sqrt{16} > \sqrt[3]{64}\)}
        \choice[correct]{\(\sqrt{16} \le \sqrt[3]{64}\)}
        \choice[correct]{\(\sqrt{16} \ge \sqrt[3]{64}\)}
    \end{selectAll}
\end{question}